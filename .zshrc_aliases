gl_token=glpat-AeCwxPzwXGzz23dhKgiE

# Taskwarrior
# Leader: t
if [[ -r "$HOME/taskwarrior_extends/" ]]; then
  . ~/taskwarrior_extends/taskwarrior_add
  . ~/taskwarrior_extends/taskwarrior_modify
fi

if [[ -r "$HOME/Projekte/taskwarrior_extends/" ]]; then
  . ~/Projekte/taskwarrior_extends/taskwarrior_add
  . ~/Projekte/taskwarrior_extends/taskwarrior_modify
fi

# Xdays
# Leader: x
if [[ -r "$HOME/xdays/" ]]; then
  . ~/xdays/work
  . ~/xdays/study
  . ~/xdays/language
fi

if [[ -r "$HOME/Projekte/xdays/" ]]; then
  . ~/Projekte/xdays/work
  . ~/Projekte/xdays/study
  . ~/Projekte/xdays/language
fi

# Workflow
# Leader: w
if [[ -r "$HOME/.scripts/workflow" ]]; then
  . ~/.scripts/workflow
fi

# basic
# Leader: _
m_depth(){
  rpath=$(pwd);
  cd ~/Projekte/smierx/;
  poetry run python scripts/0008_macos_folder_depth.py;
  cd $rpath;
}

alias _hello='echo "hello world"'
alias _reload='source ~/.zshrc'
alias _server='ssh smierx@192.168.178.87'
alias _depth="m_depth "
