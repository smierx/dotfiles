if [[ -r "$HOME/.alias/general" ]]; then
  . ~/.alias/general
fi
if [[ -r "$HOME/.alias/smierx" ]]; then
  . ~/.alias/smierx
fi
if [[ -r "$HOME/.alias/workflow" ]]; then
  . ~/.alias/workflow
fi
if [[ -r "$HOME/.alias/git" ]]; then
  . ~/.alias/git
fi
if [[ -r "$HOME/.alias/zettelkasten" ]]; then
  . ~/.alias/zettelkasten
fi
if [[ -r "$HOME/.alias/docker" ]]; then
  . ~/.alias/docker
fi
