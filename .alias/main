if [[ -r "$HOME/smierx/alias/general" ]]; then
  . ~/smierx/alias/general
fi
if [[ -r "$HOME/smierx/alias/smierx" ]]; then
  . ~/smierx/alias/smierx
fi
if [[ -r "$HOME/smierx/alias/workflow" ]]; then
  . ~/smierx/alias/workflow
fi
if [[ -r "$HOME/smierx/alias/git" ]]; then
  . ~/smierx/alias/git
fi
if [[ -r "$HOME/smierx/alias/zettelkasten" ]]; then
  . ~/smierx/alias/zettelkasten
fi
if [[ -r "$HOME/smierx/alias/docker" ]]; then
  . ~/smierx/alias/docker
fi
